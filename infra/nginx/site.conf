server {
    listen 80;
    server_name _;

    # Security headers (adjust CSP for your frontend)
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Content-Security-Policy "default-src 'self'; img-src 'self' data:; script-src 'self'; style-src 'self' 'unsafe-inline'; connect-src 'self' http: https:;" always;

    client_max_body_size 20m;

    # Django at /
    location / {
        proxy_pass         http://web:8000;
        proxy_http_version 1.1;
        proxy_set_header   Host $host;
        proxy_set_header   X-Real-IP $remote_addr;
        proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_set_header   Upgrade $http_upgrade;
        proxy_set_header   Connection $connection_upgrade;
    }

    # FastAPI at /api
    location /api/ {
        proxy_pass         http://api:8001/;
        proxy_http_version 1.1;
        proxy_set_header   Host $host;
        proxy_set_header   X-Real-IP $remote_addr;
        proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_set_header   Upgrade $http_upgrade;
        proxy_set_header   Connection $connection_upgrade;
    }
}

# Example TLS server (uncomment if you provide /certs/fullchain.pem and /certs/privkey.pem)
# server {
#     listen 443 ssl http2;
#     server_name example.com;
#     ssl_certificate     /certs/fullchain.pem;
#     ssl_certificate_key /certs/privkey.pem;
#     include /etc/nginx/conf.d/security-ssl.conf;
#     location / { proxy_pass http://web:8000; }
#     location /api/ { proxy_pass http://api:8001/; }
# }
